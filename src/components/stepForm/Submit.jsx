import React from 'react'
import Container from '@material-ui/core/Container';
const Submit = () => {
    return (
        <Container maxWidth="sm" style={{ marginTop: '4rem' }}>
        <h3>Thank you for submitting, Please wait..." till the API response.
  </h3>
      </Container>
    );
}

export default Submit;
